# Hydrometeorology VN Analytics — Python Backend & Pipeline Dependencies
# Cài đặt: pip install -r requirements.txt

# --- Web Framework ---
fastapi>=0.115.0
uvicorn>=0.30.0

# --- Data Processing ---
pandas>=2.0.0
numpy>=1.26.0

# --- Database & Storage ---
duckdb>=0.10.0          # SQL engine cho chế độ SQL analysis
supabase>=2.0.0         # Supabase Python client (auth + cloud log mirror)

# --- HTTP & Networking ---
requests>=2.31.0        # Gọi OpenRouter API và Open-Meteo API
urllib3>=2.0.0          # HTTP adapter (dùng trong 01_crawl.py retry logic)

# --- Config & Utils ---
python-dotenv>=1.0.0    # Load .env file
pydantic>=2.0.0         # Request/Response models cho FastAPI
